# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local config
.claude/

# Archive files (gak perlu di-track)
*.rar
*.zip
*.7z

# Environment variables - jangan di-upload!
# (TAPI .env.example DI-COMMIT — itu template recovery, value-nya public anon key)
.env
agent/.env
agent-mac/.env
!.env.example
!agent/.env.example
!agent-mac/.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Node MSI cache buat build-agent-zip.ps1 (auto-download saat build, jangan commit)
node-installer.msi
seat-agent-windows.zip
seat-agent-mac.zip
seat-agent-mac.tar.gz
